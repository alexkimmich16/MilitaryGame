using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class SaveData
{
    /*
    public int Silver;
    public int Gems;

    public int[] Stars = new int[14];

    public int[] Jump = new int[3];
    public int[] Control = new int[3];
    public int[] Speed = new int[3];

    public int SoundVolume;
    public int MusicVolume;

    public string Name;

    public bool ShowTerms;

    public SaveData()
    {
        Debug.Log(UpgradeScript.Name);
        for (var i = 0; i < 14; i++)
        {
            if (LevelStorage.TotalLevels[i].Stars[0] == true)
            {
                Stars[i] = 3;

            }
            else if (LevelStorage.TotalLevels[i].Stars[1] == true)
            {
                Stars[i] = 2;

            }
            else if (LevelStorage.TotalLevels[i].Stars[2] == true)
            {
                Stars[i] = 1;

            }
            else
            {
                Stars[i] = 0;
            }
            //Debug.Log(i + "savedata= " + Stars[i]);
        }

        Name = UpgradeScript.Name;

        Jump[0] = UpgradeScript.Vehicles[0].JumpLevel;
        Jump[1] = UpgradeScript.Vehicles[1].JumpLevel;

        Control[0] = UpgradeScript.Vehicles[0].ControlLevel;
        Control[1] = UpgradeScript.Vehicles[1].ControlLevel;

        Speed[0] = UpgradeScript.Vehicles[0].SpeedLevel;
        Speed[1] = UpgradeScript.Vehicles[1].SpeedLevel;

        Silver = UpgradeScript.Money;
        Gems = UpgradeScript.Gems;
        SoundVolume = Options.VolumePercent;
        MusicVolume = Options.VolumePercent2;

        ShowTerms = PolicyMenu.ShowMenu;
    }
    */
}
