using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VRscript : MonoBehaviour
{
    /*
    public float CountEverySecond = 20;
    private float MaxTime;
    private float Timer;

    public bool UseLeft;
    public bool UseRight;

    public HandActions Left;
    public HandActions Right;

    public List<Vector3> ControllerPositionsLeft = new List<Vector3>();
    public List<Vector3> ControllerPositionsRight = new List<Vector3>();
    // Start is called before the first frame update
    void Start()
    {
        MaxTime = 1f / CountEverySecond;
    }
    // Update is called once per frame
    void Update()
    {
        Timer += Time.deltaTime;

        if(Timer > MaxTime)
        {
            if (UseLeft == true)
            {
                if (Left.Trigger > HandMagic.instance.TriggerThreshold)
                {
                    ControllerPositionsLeft.Add(Left.transform.position);
                }
            }
            if (UseRight == true)
            {
                if (Right.Trigger > HandMagic.instance.TriggerThreshold)
                {
                    ControllerPositionsRight.Add(Right.transform.position);
                }
            }
            Timer = 0f;
        }
    }
    */
}
